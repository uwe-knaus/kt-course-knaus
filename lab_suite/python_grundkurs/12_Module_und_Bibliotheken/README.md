# 12 – Module und Bibliotheken

## Lernziele (Gerüst)

- **Import:** `import math`, `from math import log2`, Nutzung z. B. `math.sqrt`, `log2(2)`.
- **Standardbibliothek:** `math` für Logarithmus, Wurzel, etc.
- **Externe Bibliotheken (Kurz):** `numpy`, `matplotlib` – Installation mit `pip`, einfache Nutzung (Arrays, Plot). Details in späteren Kapiteln oder in KT-Labs.

## Material (geplant)

- Beispiele: Entropie mit `math.log2`; optional erster einfacher Plot mit `matplotlib.pyplot`.
- Hinweis auf .venv und requirements.txt im KT-workspace.

## Anknüpfung KT

- **01.2 Informationstheorie:** Formeln mit `math.log2`.
- **02.Signale Spektren:** Arrays (`numpy`), einfache Plots (`matplotlib`) – Einstieg für spätere Signal- und Spektraldarstellung.
